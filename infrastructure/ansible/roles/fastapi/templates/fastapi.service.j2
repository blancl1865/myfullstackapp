[Unit]
Description=fastapi application
After=network.target

[Service]
User=ec2-User
WorkingDirectory=/opt/myfullstackapp/backend
ExecStart=/usr/bin/python3 -m uvicorn app.app:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target